/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Enables React's strict mode, helps with debugging
  swcMinify: true,       // Enables SWC-based minification for faster builds
};

export default nextConfig;

